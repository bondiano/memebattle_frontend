const initialState = {
    uid: '',
    username: '',
    avatar: '',
    memcoin: '',
    placeInRating: '',
    accesToken: '',
    refreshToken:''
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};